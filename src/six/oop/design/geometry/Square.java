package six.oop.design.geometry;

public class Square extends Rectangle{
    public Square(int x, int y, String color, String name, double length) {
        super(x, y, color, name, length, length);
    }
}
